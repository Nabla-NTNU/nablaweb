# Ikke kjør denne filen, skriv kommandoene. 
# Selv om det kanskje funker å bare kjøre den.
# Denne filen inneholder diverse som kan være forskjellig på hver maskin

# Markdown extensions #
cd markdown_extensions
sudo cp *.py /usr/local/lib/python2.7/dist-packages/markdown/extensions
# ELLER
sudo cp *.py /usr/lib/pymodules/python2.7/markdown/extensions
# avhengig av hvilken mappe som eksisterer

# Django-image-cropping trenger noen static-filer. Om du bruker staticfiles-
# appen er dette unødvendig.
cd ../static
ln -s /usr/local/lib/python2.7/dist-packages/image_cropping/static/image_cropping image_cropping

# Om du ikke bruker nginx, må du også legge til admin-static filene
# cd ../static
ln -s /usr/local/lib/python2.7/dist-packages/django/contrib/admin/static/admin admin
